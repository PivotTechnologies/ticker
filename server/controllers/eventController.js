const Event = require('../models/eventModel');

module.exports = {
    buyerSearch: (req, res) => {
      res.send('buyerSearch');
    },
    sellerSearch: (req, res) => {
      res.send('sellerSearch');
    }
}
